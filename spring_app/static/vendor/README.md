# Local Three.js vendor files

Place these files here to run the 3D preview without external CDNs:

- `three.min.js` (Three.js r164 or compatible)
- `OrbitControls.js` from `examples/js/controls/OrbitControls.js`

The app will try local files first, then CDN fallbacks.
